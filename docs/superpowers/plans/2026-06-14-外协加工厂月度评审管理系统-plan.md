# 外协加工厂月度评审管理系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一套外协加工厂月度评审管理系统，实现工厂档案管理、月度评分（70通用+30专项）、自动评级与红黄牌、月度评审大盘、岗位 KPI 自动统计，按角色严格锁权限，最终云端部署。

**Architecture:** Vue3 + TypeScript + Vite 前端，PocketBase（v0.23+ app API）做后端/数据库/鉴权/附件。集合通过 `pb_migrations` 声明式创建，业务规则（产值锁权、评分自动算分、红黄牌、KPI 记录）通过 `pb_hooks` 服务端钩子强制执行。前端按 Pinia store 分模块，视图按角色路由守卫控制访问。

**Tech Stack:** Vue3, TypeScript, Vite, Pinia, Vue Router, PocketBase JS SDK, PocketBase v0.23+ migrations & hooks (JS), Vitest 单测。

**关键约定（沿用现有 uv-print-manager 项目）：**
- 迁移用 `migrate((app) => {...}, (app) => {...})`，集合操作用 `new Collection()`、`app.save(collection)`、`app.findCollectionByNameOrId()`
- 钩子用 `onRecordCreate((e) => {...}, "collectionName")`，链式放行用 `return e.next()`，报错用 `throw new ApiError(400, "...")`，DB 查询用 `$app.findRecordsByFilter(...)`
- pocketbase 二进制放 `bin/pocketbase`（macOS 本地开发）
- 必填数字字段注意"0 陷阱"：PocketBase number 字段 required 时 0 会被当空值，库存/金额类字段不要设 required，用 hook 或默认值兜底

---

## File Structure

```
加工厂月度评审管理制度/
├── bin/pocketbase                          # PB 二进制（本地，gitignore）
├── pb_migrations/                          # 集合声明（按编号顺序执行）
│   ├── 1700000001_create_users_fields.js
│   ├── 1700000002_create_factories.js
│   ├── 1700000003_create_monthly_output.js
│   ├── 1700000004_create_incidents.js
│   ├── 1700000005_create_score_templates.js
│   ├── 1700000006_create_monthly_scores.js
│   ├── 1700000007_create_review_meetings.js
│   ├── 1700000008_create_kpi_logs.js
│   └── 1700000009_seed_score_templates.js  # 默认评分项种子数据
├── pb_hooks/
│   ├── monthly_output_guard.pb.js          # 产值仅财务可写
│   ├── score_calc.pb.js                    # 提交评分时自动算总分+评级
│   ├── flag_guard.pb.js                    # 红黄牌仅经理可审批生效
│   ├── factory_status_guard.pb.js          # 合作状态需经理审批
│   └── kpi_logger.pb.js                    # 自动记录岗位履职动作
├── src/
│   ├── pb.ts                               # PocketBase 客户端单例
│   ├── main.ts
│   ├── App.vue
│   ├── router/index.ts                     # 路由 + 角色守卫
│   ├── types/                              # TS 类型（每个集合一个文件）
│   │   ├── user.ts
│   │   ├── factory.ts
│   │   ├── output.ts
│   │   ├── incident.ts
│   │   ├── score.ts
│   │   ├── review.ts
│   │   └── kpi.ts
│   ├── constants/
│   │   ├── roles.ts                        # 角色、工艺、权限映射
│   │   └── grading.ts                      # 评级线、红黄牌规则
│   ├── stores/                             # Pinia，每集合一个
│   │   ├── auth.ts
│   │   ├── factories.ts
│   │   ├── output.ts
│   │   ├── incidents.ts
│   │   ├── scoreTemplates.ts
│   │   ├── scores.ts
│   │   ├── reviews.ts
│   │   └── kpi.ts
│   ├── utils/
│   │   ├── grading.ts                      # 算总分+评级（前端镜像，与 hook 一致）
│   │   └── permissions.ts                  # can(role, action) 判断
│   ├── components/
│   │   ├── AppLayout.vue
│   │   ├── RoleGate.vue                    # 按角色显隐子元素
│   │   └── ...（各表单/卡片组件）
│   └── views/                              # 见设计文档第7节
│       ├── LoginView.vue
│       ├── DashboardView.vue
│       ├── FactoryListView.vue
│       ├── FactoryDetailView.vue
│       ├── ScoreSheetView.vue
│       ├── MonthlyOutputView.vue
│       ├── ReviewBoardView.vue
│       ├── ReviewMeetingView.vue
│       ├── KpiBoardView.vue
│       └── admin/
│           ├── ScoreTemplateAdminView.vue
│           └── UserAdminView.vue
├── tests/                                  # Vitest
├── package.json
├── vite.config.ts
├── tsconfig.json
└── .gitignore
```

---

## Phase 0 — 项目脚手架

### Task 0.1: 初始化前端工程

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.ts`, `src/App.vue`, `.gitignore`

- [ ] **Step 1: 用 Vite 创建 Vue+TS 工程**

Run（在项目根目录）：
```bash
cd "/Users/wendy/Desktop/加工厂月度评审管理制度"
npm create vite@latest . -- --template vue-ts
```
若提示目录非空（已有 docs/、.git/），选择 "Ignore files and continue"。

- [ ] **Step 2: 安装依赖**

Run:
```bash
npm install
npm install pocketbase pinia vue-router
npm install -D vitest @vue/test-utils jsdom
```

- [ ] **Step 3: 配置 vitest**

在 `vite.config.ts` 的 defineConfig 中加入：
```ts
/// <reference types="vitest" />
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  test: {
    environment: 'jsdom',
    globals: true,
  },
})
```

并在 `package.json` 的 scripts 加：
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: 写 .gitignore**

```
node_modules/
dist/
bin/
pb_data/
*.log
.DS_Store
.env
```

- [ ] **Step 5: 验证工程能跑**

Run: `npm run dev`
Expected: Vite 启动，浏览器打开显示默认 Vue 页面，无报错。Ctrl+C 停止。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: scaffold Vue3+TS+Vite project with pinia/router/pocketbase/vitest"
```

---

### Task 0.2: 下载 PocketBase 二进制并启动

**Files:**
- Create: `bin/pocketbase`（gitignored）

- [ ] **Step 1: 下载 PocketBase（macOS arm64）**

Run:
```bash
cd "/Users/wendy/Desktop/加工厂月度评审管理制度"
mkdir -p bin
curl -L -o /tmp/pb.zip https://github.com/pocketbase/pocketbase/releases/download/v0.23.0/pocketbase_0.23.0_darwin_arm64.zip
unzip -o /tmp/pb.zip -d bin/
chmod +x bin/pocketbase
```
（若是 Intel Mac 换 `darwin_amd64`）

- [ ] **Step 2: 首次启动并建管理员**

Run:
```bash
bin/pocketbase serve
```
Expected: 输出 `Server started at http://127.0.0.1:8090`，访问 `http://127.0.0.1:8090/_/` 打开管理后台，创建超级管理员账号（记下邮箱/密码）。Ctrl+C 停止。

- [ ] **Step 3: 创建 PocketBase 客户端单例**

Create `src/pb.ts`:
```ts
import PocketBase from 'pocketbase'

// 本地开发指向 8090；生产由 Nginx 反代到同源 /，此时用相对地址
const baseUrl = import.meta.env.DEV ? 'http://127.0.0.1:8090' : '/'
export const pb = new PocketBase(baseUrl)
pb.autoCancellation(false)
```

- [ ] **Step 4: Commit**

```bash
git add src/pb.ts
git commit -m "chore: add PocketBase client singleton and bin setup notes"
```

---

## Phase 1 — 后端集合（迁移）

> 每个迁移文件写完后用 `bin/pocketbase migrate up` 应用，并在管理后台确认集合/字段已生成。

### Task 1.1: 扩展 users 集合字段（role / craft / display_name）

**Files:**
- Create: `pb_migrations/1700000001_create_users_fields.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000001_create_users_fields.js
// 给内置 users 集合增加 role / craft / display_name 字段
migrate((app) => {
  const users = app.findCollectionByNameOrId('users')

  users.fields.add(new Field({
    name: 'role',
    type: 'select',
    required: true,
    maxSelect: 1,
    values: [
      'admin', 'sc_manager',
      'buyer_injection', 'buyer_painting', 'buyer_assembly', 'buyer_sewing',
      'finance_cost', 'finance_mgr', 'quality_qc', 'sc_clerk',
    ],
  }))

  users.fields.add(new Field({
    name: 'craft',
    type: 'select',
    required: false,
    maxSelect: 1,
    values: ['injection', 'painting', 'assembly', 'sewing'],
  }))

  users.fields.add(new Field({
    name: 'display_name',
    type: 'text',
    required: false,
  }))

  app.save(users)
}, (app) => {
  const users = app.findCollectionByNameOrId('users')
  for (const fieldName of ['role', 'craft', 'display_name']) {
    const f = users.fields.find((x) => x.name === fieldName)
    if (f) users.fields.removeById(f.id)
  }
  app.save(users)
})
```

- [ ] **Step 2: 应用迁移**

Run: `bin/pocketbase migrate up`
Expected: 输出应用了 `1700000001...` 一条迁移，无报错。

- [ ] **Step 3: 后台确认**

启动 `bin/pocketbase serve`，在 `/_/` → Collections → users，确认出现 `role`、`craft`、`display_name` 三个字段。

- [ ] **Step 4: Commit**

```bash
git add pb_migrations/1700000001_create_users_fields.js
git commit -m "feat(db): add role/craft/display_name fields to users"
```

---

### Task 1.2: 创建 factories 集合（工厂档案 — 表1）

**Files:**
- Create: `pb_migrations/1700000002_create_factories.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000002_create_factories.js
migrate((app) => {
  const c = new Collection({
    type: 'base',
    name: 'factories',
    fields: [
      { name: 'name', type: 'text', required: true },
      { name: 'craft', type: 'select', required: true, maxSelect: 1,
        values: ['injection', 'painting', 'assembly', 'sewing'] },
      { name: 'contact_person', type: 'text' },
      { name: 'contact_phone', type: 'text' },
      { name: 'address', type: 'text' },
      { name: 'workshop_area', type: 'number' },
      { name: 'equipment_list', type: 'json', maxSize: 200000 },
      { name: 'qualification_files', type: 'file', maxSelect: 20, maxSize: 10485760 },
      { name: 'qualification_expiry', type: 'date' },
      { name: 'status', type: 'select', required: true, maxSelect: 1,
        values: ['active', 'limited', 'suspended', 'eliminated'] },
      { name: 'status_pending', type: 'select', maxSelect: 1,
        values: ['active', 'limited', 'suspended', 'eliminated'] },
      { name: 'status_updated_by', type: 'relation', maxSelect: 1,
        collectionId: app.findCollectionByNameOrId('users').id },
      { name: 'status_updated_at', type: 'date' },
      { name: 'created_by', type: 'relation', maxSelect: 1,
        collectionId: app.findCollectionByNameOrId('users').id },
    ],
    // 登录用户可读；写权限由 hook + 前端控制（细到字段，规则层先放给登录用户）
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.id != ""',
    updateRule: '@request.auth.id != ""',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('factories'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: 应用 `1700000002`，后台出现 `factories` 集合及全部字段。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000002_create_factories.js
git commit -m "feat(db): create factories collection (表1 工厂档案)"
```

---

### Task 1.3: 创建 monthly_output 集合（月度产值）

**Files:**
- Create: `pb_migrations/1700000003_create_monthly_output.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000003_create_monthly_output.js
migrate((app) => {
  const usersId = app.findCollectionByNameOrId('users').id
  const factoriesId = app.findCollectionByNameOrId('factories').id
  const c = new Collection({
    type: 'base',
    name: 'monthly_output',
    fields: [
      { name: 'factory', type: 'relation', required: true, maxSelect: 1, collectionId: factoriesId },
      { name: 'year_month', type: 'text', required: true }, // "2026-05"
      // 金额不设 required，避免 0 陷阱；缺省由前端校验
      { name: 'monthly_amount', type: 'number' },
      { name: 'ytd_amount', type: 'number' },
      { name: 'source_doc', type: 'text' },
      { name: 'entered_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'reviewed_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'entered_at', type: 'date' },
      { name: 'reviewed_at', type: 'date' },
    ],
    indexes: ['CREATE UNIQUE INDEX idx_output_factory_month ON monthly_output (factory, year_month)'],
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.id != ""',
    updateRule: '@request.auth.id != ""',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('monthly_output'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: `monthly_output` 集合创建，含唯一索引 (factory, year_month)。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000003_create_monthly_output.js
git commit -m "feat(db): create monthly_output collection with unique factory-month index"
```

---

### Task 1.4: 创建 incidents 集合（异常/事故）

**Files:**
- Create: `pb_migrations/1700000004_create_incidents.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000004_create_incidents.js
migrate((app) => {
  const usersId = app.findCollectionByNameOrId('users').id
  const factoriesId = app.findCollectionByNameOrId('factories').id
  const c = new Collection({
    type: 'base',
    name: 'incidents',
    fields: [
      { name: 'factory', type: 'relation', required: true, maxSelect: 1, collectionId: factoriesId },
      { name: 'incident_date', type: 'date', required: true },
      { name: 'incident_type', type: 'select', required: true, maxSelect: 1,
        values: ['batch_defect', 'env_violation', 'shutdown', 'other'] },
      { name: 'description', type: 'editor' },
      { name: 'photos', type: 'file', maxSelect: 20, maxSize: 10485760 },
      { name: 'docs', type: 'file', maxSelect: 20, maxSize: 10485760 },
      { name: 'entered_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'reviewed_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'status', type: 'select', required: true, maxSelect: 1, values: ['open', 'closed'] },
      { name: 'close_date', type: 'date' },
    ],
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.id != ""',
    updateRule: '@request.auth.id != ""',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('incidents'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: `incidents` 集合创建成功。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000004_create_incidents.js
git commit -m "feat(db): create incidents collection with photo/doc attachments"
```

---

### Task 1.5: 创建 score_templates 集合（评分项模板）

**Files:**
- Create: `pb_migrations/1700000005_create_score_templates.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000005_create_score_templates.js
migrate((app) => {
  const c = new Collection({
    type: 'base',
    name: 'score_templates',
    fields: [
      { name: 'name', type: 'text', required: true },
      { name: 'module', type: 'select', required: true, maxSelect: 1,
        values: ['qualification', 'delivery', 'cooperation', 'defect_rate', 'process', '5s', 'craft_specific'] },
      { name: 'max_score', type: 'number', required: true },
      { name: 'scoring_role', type: 'select', required: true, maxSelect: 1,
        values: ['buyer', 'quality_qc'] },
      { name: 'craft_filter', type: 'select', maxSelect: 1,
        values: ['injection', 'painting', 'assembly', 'sewing'] }, // 空=通用
      { name: 'is_active', type: 'bool' },
      { name: 'sort_order', type: 'number' },
      { name: 'description', type: 'editor' },
    ],
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.role = "admin"',
    updateRule: '@request.auth.role = "admin"',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('score_templates'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: `score_templates` 集合创建，仅 admin 可增改删。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000005_create_score_templates.js
git commit -m "feat(db): create configurable score_templates collection (admin-only write)"
```

---

### Task 1.6: 创建 monthly_scores 集合（月度评分 — 表2）

**Files:**
- Create: `pb_migrations/1700000006_create_monthly_scores.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000006_create_monthly_scores.js
migrate((app) => {
  const usersId = app.findCollectionByNameOrId('users').id
  const factoriesId = app.findCollectionByNameOrId('factories').id
  const c = new Collection({
    type: 'base',
    name: 'monthly_scores',
    fields: [
      { name: 'factory', type: 'relation', required: true, maxSelect: 1, collectionId: factoriesId },
      { name: 'year_month', type: 'text', required: true },
      // [{ template_id, score, notes }]
      { name: 'score_items', type: 'json', maxSize: 200000 },
      { name: 'total_score', type: 'number' },
      { name: 'grade', type: 'select', maxSelect: 1, values: ['A', 'B', 'C', 'D'] },
      { name: 'flag', type: 'select', maxSelect: 1, values: ['none', 'yellow', 'red'] },
      { name: 'flag_reason', type: 'editor' },
      { name: 'flag_issued_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'flag_approved_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'correction_plan', type: 'editor' },
      { name: 'correction_due', type: 'date' },
      { name: 'correction_closed', type: 'bool' },
      { name: 'status', type: 'select', required: true, maxSelect: 1,
        values: ['draft', 'submitted', 'approved'] },
      { name: 'submitted_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'approved_by', type: 'relation', maxSelect: 1, collectionId: usersId },
    ],
    indexes: ['CREATE UNIQUE INDEX idx_score_factory_month ON monthly_scores (factory, year_month)'],
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.id != ""',
    updateRule: '@request.auth.id != ""',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('monthly_scores'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: `monthly_scores` 集合创建，含唯一索引 (factory, year_month)。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000006_create_monthly_scores.js
git commit -m "feat(db): create monthly_scores collection (表2 评分)"
```

---

### Task 1.7: 创建 review_meetings 集合（月度评审 — 表3）

**Files:**
- Create: `pb_migrations/1700000007_create_review_meetings.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000007_create_review_meetings.js
migrate((app) => {
  const usersId = app.findCollectionByNameOrId('users').id
  const c = new Collection({
    type: 'base',
    name: 'review_meetings',
    fields: [
      { name: 'year_month', type: 'text', required: true },
      // { [craft]: { factory_count, grade_dist:{A,B,C,D}, avg_score, total_output } }
      { name: 'summary_by_craft', type: 'json', maxSize: 200000 },
      { name: 'optimization_suggestions', type: 'editor' },
      { name: 'participants', type: 'json', maxSize: 50000 },
      { name: 'meeting_date', type: 'date' },
      { name: 'approved_by', type: 'relation', maxSelect: 1, collectionId: usersId },
      { name: 'status', type: 'select', required: true, maxSelect: 1, values: ['draft', 'approved'] },
    ],
    indexes: ['CREATE UNIQUE INDEX idx_review_month ON review_meetings (year_month)'],
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.id != ""',
    updateRule: '@request.auth.id != ""',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('review_meetings'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: `review_meetings` 集合创建，year_month 唯一。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000007_create_review_meetings.js
git commit -m "feat(db): create review_meetings collection (表3 评审大盘)"
```

---

### Task 1.8: 创建 kpi_logs 集合（岗位履职记录）

**Files:**
- Create: `pb_migrations/1700000008_create_kpi_logs.js`

- [ ] **Step 1: 写迁移**

```js
// pb_migrations/1700000008_create_kpi_logs.js
migrate((app) => {
  const usersId = app.findCollectionByNameOrId('users').id
  const c = new Collection({
    type: 'base',
    name: 'kpi_logs',
    fields: [
      { name: 'user', type: 'relation', required: true, maxSelect: 1, collectionId: usersId },
      { name: 'action_type', type: 'select', required: true, maxSelect: 1,
        values: ['output_entered', 'output_reviewed', 'score_submitted', 'summary_done', 'correction_closed'] },
      { name: 'target_month', type: 'text', required: true },
      { name: 'deadline', type: 'date' },
      { name: 'completed_at', type: 'date' },
      { name: 'is_on_time', type: 'bool' },
      { name: 'notes', type: 'text' },
    ],
    listRule: '@request.auth.id != ""',
    viewRule: '@request.auth.id != ""',
    createRule: '@request.auth.id != ""',
    updateRule: '@request.auth.role = "admin"',
    deleteRule: '@request.auth.role = "admin"',
  })
  app.save(c)
}, (app) => {
  app.delete(app.findCollectionByNameOrId('kpi_logs'))
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: `kpi_logs` 集合创建。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000008_create_kpi_logs.js
git commit -m "feat(db): create kpi_logs collection for position KPI tracking"
```

---

### Task 1.9: 种子默认评分模板

**Files:**
- Create: `pb_migrations/1700000009_seed_score_templates.js`

- [ ] **Step 1: 写种子迁移**

```js
// pb_migrations/1700000009_seed_score_templates.js
// 注入默认通用 70 分评分项（工艺专项 30 分由 admin 在后台配置）
migrate((app) => {
  const col = app.findCollectionByNameOrId('score_templates')
  const seeds = [
    { name: '证件齐全有效', module: 'qualification', max_score: 10, scoring_role: 'buyer', sort_order: 1 },
    { name: '交期准时率',   module: 'delivery',      max_score: 20, scoring_role: 'buyer', sort_order: 2 },
    { name: '整改响应配合度', module: 'cooperation',  max_score: 10, scoring_role: 'buyer', sort_order: 3 },
    { name: '月度综合不良率', module: 'defect_rate',  max_score: 15, scoring_role: 'quality_qc', sort_order: 4 },
    { name: '制程检验通过率', module: 'process',      max_score: 10, scoring_role: 'quality_qc', sort_order: 5 },
    { name: '5S现场评分',    module: '5s',           max_score: 5,  scoring_role: 'quality_qc', sort_order: 6 },
  ]
  for (const s of seeds) {
    const rec = new Record(col, {
      name: s.name, module: s.module, max_score: s.max_score,
      scoring_role: s.scoring_role, craft_filter: '', is_active: true, sort_order: s.sort_order,
    })
    app.save(rec)
  }
}, (app) => {
  const recs = app.findRecordsByFilter('score_templates', 'craft_filter = ""', '', 0, 0)
  for (const r of recs) app.delete(r)
})
```

- [ ] **Step 2: 应用并确认**

Run: `bin/pocketbase migrate up`
Expected: 后台 `score_templates` 出现 6 条通用评分项，合计满分 70。

- [ ] **Step 3: Commit**

```bash
git add pb_migrations/1700000009_seed_score_templates.js
git commit -m "feat(db): seed default 70-point general score templates"
```

---

## Phase 2 — 前端基础：常量、类型、鉴权、权限

### Task 2.1: 角色与评级常量

**Files:**
- Create: `src/constants/roles.ts`, `src/constants/grading.ts`
- Test: `tests/grading.spec.ts`

- [ ] **Step 1: 写角色常量**

Create `src/constants/roles.ts`:
```ts
export type Role =
  | 'admin' | 'sc_manager'
  | 'buyer_injection' | 'buyer_painting' | 'buyer_assembly' | 'buyer_sewing'
  | 'finance_cost' | 'finance_mgr' | 'quality_qc' | 'sc_clerk'

export type Craft = 'injection' | 'painting' | 'assembly' | 'sewing'

export const ROLE_LABELS: Record<Role, string> = {
  admin: '管理员', sc_manager: '供应链经理',
  buyer_injection: '采购专员-注塑', buyer_painting: '采购专员-喷油',
  buyer_assembly: '采购专员-装配', buyer_sewing: '采购专员-毛绒车缝',
  finance_cost: '财务成本会计', finance_mgr: '财务主管',
  quality_qc: '品质QC', sc_clerk: '供应链文员',
}

export const CRAFT_LABELS: Record<Craft, string> = {
  injection: '注塑', painting: '喷油', assembly: '装配', sewing: '毛绒车缝',
}

// 采购角色 → 其负责的工艺
export const BUYER_CRAFT: Partial<Record<Role, Craft>> = {
  buyer_injection: 'injection', buyer_painting: 'painting',
  buyer_assembly: 'assembly', buyer_sewing: 'sewing',
}

export function isBuyer(role: Role): boolean {
  return role.startsWith('buyer_')
}
```

- [ ] **Step 2: 写失败测试（评级函数）**

Create `tests/grading.spec.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { gradeFromScore } from '../src/utils/grading'

describe('gradeFromScore', () => {
  it('returns A for >= 90', () => expect(gradeFromScore(90)).toBe('A'))
  it('returns B for 80..89', () => expect(gradeFromScore(85)).toBe('B'))
  it('returns C for 70..79', () => expect(gradeFromScore(70)).toBe('C'))
  it('returns D for < 70', () => expect(gradeFromScore(69.9)).toBe('D'))
})
```

- [ ] **Step 3: 运行测试确认失败**

Run: `npm test -- grading`
Expected: FAIL（`gradeFromScore` 未定义 / 模块不存在）

- [ ] **Step 4: 写评级常量 + 工具函数**

Create `src/constants/grading.ts`:
```ts
export type Grade = 'A' | 'B' | 'C' | 'D'
// 评级线（满分100）：>=90 A正常, 80-89 B正常, 70-79 C限单, <70 D暂停/淘汰评审
export const GRADE_THRESHOLDS: { min: number; grade: Grade }[] = [
  { min: 90, grade: 'A' },
  { min: 80, grade: 'B' },
  { min: 70, grade: 'C' },
  { min: 0, grade: 'D' },
]
// 红牌触发的异常类型
export const RED_FLAG_INCIDENTS = ['batch_defect', 'env_violation', 'shutdown']
```

Create `src/utils/grading.ts`:
```ts
import { GRADE_THRESHOLDS, type Grade } from '../constants/grading'

export function gradeFromScore(total: number): Grade {
  for (const t of GRADE_THRESHOLDS) {
    if (total >= t.min) return t.grade
  }
  return 'D'
}

// 总分 = 各项 score 之和（前端镜像，与服务端 hook 保持一致）
export function totalFromItems(items: { score: number }[]): number {
  return items.reduce((sum, it) => sum + (Number(it.score) || 0), 0)
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `npm test -- grading`
Expected: PASS（4 个用例全过）

- [ ] **Step 6: Commit**

```bash
git add src/constants/ src/utils/grading.ts tests/grading.spec.ts
git commit -m "feat(fe): add roles/grading constants and gradeFromScore util with tests"
```

---

### Task 2.2: 权限判断工具

**Files:**
- Create: `src/utils/permissions.ts`
- Test: `tests/permissions.spec.ts`

- [ ] **Step 1: 写失败测试**

Create `tests/permissions.spec.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { canEditOutput, canApproveStatus, visibleCraft } from '../src/utils/permissions'

describe('permissions', () => {
  it('only finance_cost can edit output', () => {
    expect(canEditOutput('finance_cost')).toBe(true)
    expect(canEditOutput('buyer_injection')).toBe(false)
    expect(canEditOutput('admin')).toBe(true)
  })
  it('only sc_manager/admin approve status', () => {
    expect(canApproveStatus('sc_manager')).toBe(true)
    expect(canApproveStatus('buyer_injection')).toBe(false)
  })
  it('buyer sees only own craft, others see all', () => {
    expect(visibleCraft('buyer_painting')).toBe('painting')
    expect(visibleCraft('sc_manager')).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- permissions`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 写实现**

Create `src/utils/permissions.ts`:
```ts
import type { Role, Craft } from '../constants/roles'
import { BUYER_CRAFT } from '../constants/roles'

export function canEditOutput(role: Role): boolean {
  return role === 'finance_cost' || role === 'admin'
}
export function canApproveStatus(role: Role): boolean {
  return role === 'sc_manager' || role === 'admin'
}
export function canApproveScore(role: Role): boolean {
  return role === 'sc_manager' || role === 'admin'
}
export function canEditTemplates(role: Role): boolean {
  return role === 'admin'
}
// 返回该角色只能看的工艺；null = 看全部
export function visibleCraft(role: Role): Craft | null {
  return BUYER_CRAFT[role] ?? null
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- permissions`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/permissions.ts tests/permissions.spec.ts
git commit -m "feat(fe): add role-based permission helpers with tests"
```

---

### Task 2.3: TS 类型定义

**Files:**
- Create: `src/types/user.ts`, `factory.ts`, `output.ts`, `incident.ts`, `score.ts`, `review.ts`, `kpi.ts`

- [ ] **Step 1: 写类型文件**

Create `src/types/user.ts`:
```ts
import type { Role, Craft } from '../constants/roles'
export interface AppUser {
  id: string
  email: string
  role: Role
  craft?: Craft
  display_name?: string
}
```

Create `src/types/factory.ts`:
```ts
import type { Craft } from '../constants/roles'
export type FactoryStatus = 'active' | 'limited' | 'suspended' | 'eliminated'
export interface Factory {
  id: string
  name: string
  craft: Craft
  contact_person?: string
  contact_phone?: string
  address?: string
  workshop_area?: number
  equipment_list?: string[]
  qualification_files?: string[]
  qualification_expiry?: string
  status: FactoryStatus
  status_pending?: FactoryStatus | ''
  status_updated_by?: string
  status_updated_at?: string
  created_by?: string
}
```

Create `src/types/output.ts`:
```ts
export interface MonthlyOutput {
  id: string
  factory: string
  year_month: string
  monthly_amount?: number
  ytd_amount?: number
  source_doc?: string
  entered_by?: string
  reviewed_by?: string
  entered_at?: string
  reviewed_at?: string
}
```

Create `src/types/incident.ts`:
```ts
export type IncidentType = 'batch_defect' | 'env_violation' | 'shutdown' | 'other'
export interface Incident {
  id: string
  factory: string
  incident_date: string
  incident_type: IncidentType
  description?: string
  photos?: string[]
  docs?: string[]
  entered_by?: string
  reviewed_by?: string
  status: 'open' | 'closed'
  close_date?: string
}
```

Create `src/types/score.ts`:
```ts
import type { Grade } from '../constants/grading'
import type { Craft } from '../constants/roles'

export type ScoreModule =
  | 'qualification' | 'delivery' | 'cooperation'
  | 'defect_rate' | 'process' | '5s' | 'craft_specific'

export interface ScoreTemplate {
  id: string
  name: string
  module: ScoreModule
  max_score: number
  scoring_role: 'buyer' | 'quality_qc'
  craft_filter?: Craft | ''
  is_active: boolean
  sort_order: number
  description?: string
}

export interface ScoreItem {
  template_id: string
  score: number
  notes?: string
}

export interface MonthlyScore {
  id: string
  factory: string
  year_month: string
  score_items: ScoreItem[]
  total_score?: number
  grade?: Grade
  flag: 'none' | 'yellow' | 'red'
  flag_reason?: string
  flag_issued_by?: string
  flag_approved_by?: string
  correction_plan?: string
  correction_due?: string
  correction_closed?: boolean
  status: 'draft' | 'submitted' | 'approved'
  submitted_by?: string
  approved_by?: string
}
```

Create `src/types/review.ts`:
```ts
import type { Craft } from '../constants/roles'
export interface CraftSummary {
  factory_count: number
  grade_dist: { A: number; B: number; C: number; D: number }
  avg_score: number
  total_output: number
}
export interface ReviewMeeting {
  id: string
  year_month: string
  summary_by_craft: Partial<Record<Craft, CraftSummary>>
  optimization_suggestions?: string
  participants?: string[]
  meeting_date?: string
  approved_by?: string
  status: 'draft' | 'approved'
}
```

Create `src/types/kpi.ts`:
```ts
export type KpiAction =
  | 'output_entered' | 'output_reviewed' | 'score_submitted'
  | 'summary_done' | 'correction_closed'
export interface KpiLog {
  id: string
  user: string
  action_type: KpiAction
  target_month: string
  deadline?: string
  completed_at?: string
  is_on_time?: boolean
  notes?: string
}
```

- [ ] **Step 2: 类型编译检查**

Run: `npx vue-tsc --noEmit`
Expected: 无类型错误（若 vue-tsc 未装，`npm i -D vue-tsc` 后再跑）

- [ ] **Step 3: Commit**

```bash
git add src/types/
git commit -m "feat(fe): add TypeScript types for all collections"
```

---

### Task 2.4: auth store + 登录页 + 路由守卫

**Files:**
- Create: `src/stores/auth.ts`, `src/views/LoginView.vue`, `src/router/index.ts`
- Modify: `src/main.ts`, `src/App.vue`
- Test: `tests/auth.spec.ts`

- [ ] **Step 1: 写 auth store 失败测试**

Create `tests/auth.spec.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { useAuthStore } from '../src/stores/auth'

describe('auth store', () => {
  beforeEach(() => setActivePinia(createPinia()))
  it('starts logged out', () => {
    const s = useAuthStore()
    expect(s.isLoggedIn).toBe(false)
    expect(s.role).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- auth`
Expected: FAIL（store 不存在）

- [ ] **Step 3: 写 auth store**

Create `src/stores/auth.ts`:
```ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { pb } from '../pb'
import type { Role, Craft } from '../constants/roles'

export const useAuthStore = defineStore('auth', () => {
  const userId = ref<string | null>(pb.authStore.record?.id ?? null)
  const role = ref<Role | null>((pb.authStore.record?.role as Role) ?? null)
  const craft = ref<Craft | null>((pb.authStore.record?.craft as Craft) ?? null)
  const displayName = ref<string>(pb.authStore.record?.display_name ?? '')

  const isLoggedIn = computed(() => !!userId.value)

  function sync() {
    const rec = pb.authStore.record
    userId.value = rec?.id ?? null
    role.value = (rec?.role as Role) ?? null
    craft.value = (rec?.craft as Craft) ?? null
    displayName.value = rec?.display_name ?? ''
  }

  async function login(email: string, password: string) {
    await pb.collection('users').authWithPassword(email, password)
    sync()
  }
  function logout() {
    pb.authStore.clear()
    sync()
  }
  return { userId, role, craft, displayName, isLoggedIn, login, logout, sync }
})
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- auth`
Expected: PASS

- [ ] **Step 5: 写登录页**

Create `src/views/LoginView.vue`:
```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { useAuthStore } from '../stores/auth'

const email = ref('')
const password = ref('')
const error = ref('')
const auth = useAuthStore()
const router = useRouter()

async function onSubmit() {
  error.value = ''
  try {
    await auth.login(email.value, password.value)
    router.push('/dashboard')
  } catch (e: any) {
    error.value = '登录失败：' + (e?.message ?? '账号或密码错误')
  }
}
</script>

<template>
  <div class="login">
    <h1>加工厂月度评审管理</h1>
    <form @submit.prevent="onSubmit">
      <input v-model="email" type="email" placeholder="邮箱" required />
      <input v-model="password" type="password" placeholder="密码" required />
      <button type="submit">登录</button>
      <p v-if="error" class="err">{{ error }}</p>
    </form>
  </div>
</template>
```

- [ ] **Step 6: 写路由 + 守卫**

Create `src/router/index.ts`:
```ts
import { createRouter, createWebHistory, type RouteRecordRaw } from 'vue-router'
import { useAuthStore } from '../stores/auth'
import { canEditTemplates } from '../utils/permissions'

const routes: RouteRecordRaw[] = [
  { path: '/login', component: () => import('../views/LoginView.vue'), meta: { public: true } },
  { path: '/', redirect: '/dashboard' },
  { path: '/dashboard', component: () => import('../views/DashboardView.vue') },
  { path: '/factories', component: () => import('../views/FactoryListView.vue') },
  { path: '/factories/:id', component: () => import('../views/FactoryDetailView.vue') },
  { path: '/factories/:id/score/:month', component: () => import('../views/ScoreSheetView.vue') },
  { path: '/monthly-output', component: () => import('../views/MonthlyOutputView.vue') },
  { path: '/review/:month', component: () => import('../views/ReviewBoardView.vue') },
  { path: '/review/:month/meeting', component: () => import('../views/ReviewMeetingView.vue') },
  { path: '/kpi', component: () => import('../views/KpiBoardView.vue') },
  { path: '/admin/score-templates', component: () => import('../views/admin/ScoreTemplateAdminView.vue'), meta: { adminOnly: true } },
  { path: '/admin/users', component: () => import('../views/admin/UserAdminView.vue'), meta: { adminOnly: true } },
]

export const router = createRouter({ history: createWebHistory(), routes })

router.beforeEach((to) => {
  const auth = useAuthStore()
  if (!to.meta.public && !auth.isLoggedIn) return '/login'
  if (to.meta.adminOnly && !(auth.role && canEditTemplates(auth.role))) return '/dashboard'
  return true
})
```

- [ ] **Step 7: 接线 main.ts 与 App.vue**

Replace `src/main.ts`:
```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import { router } from './router'

createApp(App).use(createPinia()).use(router).mount('#app')
```

Replace `src/App.vue`:
```vue
<script setup lang="ts">
import { RouterView } from 'vue-router'
</script>
<template>
  <RouterView />
</template>
```

- [ ] **Step 8: 手动验证登录流程**

先在 PocketBase 后台手动建一个 role=admin 的 users 记录（设 display_name），然后 `npm run dev` + `bin/pocketbase serve`，访问应用 → 自动跳 /login → 输入该账号 → 成功跳 /dashboard（DashboardView 还没建，下一阶段做，先确认跳转不报错）。

- [ ] **Step 9: Commit**

```bash
git add src/stores/auth.ts src/views/LoginView.vue src/router/index.ts src/main.ts src/App.vue tests/auth.spec.ts
git commit -m "feat(fe): add auth store, login view, router with role guards"
```

---

### Task 2.5: 应用骨架布局 + 导航（按角色显隐）

**Files:**
- Create: `src/components/AppLayout.vue`, `src/components/RoleGate.vue`, `src/views/DashboardView.vue`

- [ ] **Step 1: 写 RoleGate 组件**

Create `src/components/RoleGate.vue`:
```vue
<script setup lang="ts">
import { computed } from 'vue'
import { useAuthStore } from '../stores/auth'
import type { Role } from '../constants/roles'

const props = defineProps<{ allow: Role[] }>()
const auth = useAuthStore()
const ok = computed(() => !!auth.role && props.allow.includes(auth.role))
</script>
<template>
  <slot v-if="ok" />
</template>
```

- [ ] **Step 2: 写布局 + 导航**

Create `src/components/AppLayout.vue`:
```vue
<script setup lang="ts">
import { RouterLink } from 'vue-router'
import { useAuthStore } from '../stores/auth'
import { ROLE_LABELS } from '../constants/roles'
import { canEditOutput, canEditTemplates } from '../utils/permissions'

const auth = useAuthStore()
</script>
<template>
  <div class="layout">
    <header>
      <strong>加工厂月度评审管理</strong>
      <nav>
        <RouterLink to="/dashboard">首页</RouterLink>
        <RouterLink to="/factories">工厂</RouterLink>
        <RouterLink v-if="auth.role && canEditOutput(auth.role)" to="/monthly-output">产值录入</RouterLink>
        <RouterLink to="/kpi">KPI看板</RouterLink>
        <RouterLink v-if="auth.role && canEditTemplates(auth.role)" to="/admin/score-templates">评分模板</RouterLink>
        <RouterLink v-if="auth.role && canEditTemplates(auth.role)" to="/admin/users">用户</RouterLink>
      </nav>
      <span class="user">
        {{ auth.displayName }}（{{ auth.role ? ROLE_LABELS[auth.role] : '' }}）
        <button @click="auth.logout()">退出</button>
      </span>
    </header>
    <main><slot /></main>
  </div>
</template>
```

- [ ] **Step 3: 写占位 DashboardView**

Create `src/views/DashboardView.vue`:
```vue
<script setup lang="ts">
import AppLayout from '../components/AppLayout.vue'
import { useAuthStore } from '../stores/auth'
const auth = useAuthStore()
</script>
<template>
  <AppLayout>
    <h2>首页</h2>
    <p>欢迎，{{ auth.displayName }}。本月待办与进度将在此显示。</p>
  </AppLayout>
</template>
```

- [ ] **Step 4: 手动验证**

`npm run dev`，用 admin 登录，确认顶部导航出现"产值录入/评分模板/用户"等按钮；退出后跳回登录页。

- [ ] **Step 5: Commit**

```bash
git add src/components/AppLayout.vue src/components/RoleGate.vue src/views/DashboardView.vue
git commit -m "feat(fe): add app layout, role-gated nav, dashboard placeholder"
```

---

## Phase 3 — 工厂档案（表1）

### Task 3.1: factories store（含按工艺过滤）

**Files:**
- Create: `src/stores/factories.ts`
- Test: `tests/factories.store.spec.ts`

- [ ] **Step 1: 写失败测试（过滤逻辑）**

Create `tests/factories.store.spec.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { filterByCraft } from '../src/stores/factories'
import type { Factory } from '../src/types/factory'

const make = (id: string, craft: Factory['craft']): Factory =>
  ({ id, name: id, craft, status: 'active' })

describe('filterByCraft', () => {
  const all = [make('a', 'injection'), make('b', 'painting')]
  it('returns all when craft is null', () => {
    expect(filterByCraft(all, null).length).toBe(2)
  })
  it('filters to one craft', () => {
    expect(filterByCraft(all, 'painting').map((f) => f.id)).toEqual(['b'])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- factories.store`
Expected: FAIL

- [ ] **Step 3: 写 store + 纯函数**

Create `src/stores/factories.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { Factory } from '../types/factory'
import type { Craft } from '../constants/roles'

export function filterByCraft(list: Factory[], craft: Craft | null): Factory[] {
  if (!craft) return list
  return list.filter((f) => f.craft === craft)
}

export const useFactoriesStore = defineStore('factories', () => {
  const items = ref<Factory[]>([])
  const loading = ref(false)

  async function fetchAll() {
    loading.value = true
    try {
      items.value = await pb.collection('factories').getFullList<Factory>({ sort: 'name' })
    } finally {
      loading.value = false
    }
  }
  async function get(id: string) {
    return pb.collection('factories').getOne<Factory>(id)
  }
  async function create(data: Partial<Factory>) {
    return pb.collection('factories').create<Factory>(data)
  }
  async function update(id: string, data: Partial<Factory>) {
    return pb.collection('factories').update<Factory>(id, data)
  }
  return { items, loading, fetchAll, get, create, update }
})
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- factories.store`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/stores/factories.ts tests/factories.store.spec.ts
git commit -m "feat(fe): add factories store with craft filtering"
```

---

### Task 3.2: 工厂列表页（按角色自动过滤工艺）

**Files:**
- Create: `src/views/FactoryListView.vue`

- [ ] **Step 1: 写列表视图**

Create `src/views/FactoryListView.vue`:
```vue
<script setup lang="ts">
import { onMounted, computed } from 'vue'
import { RouterLink } from 'vue-router'
import AppLayout from '../components/AppLayout.vue'
import { useFactoriesStore, filterByCraft } from '../stores/factories'
import { useAuthStore } from '../stores/auth'
import { visibleCraft } from '../utils/permissions'
import { CRAFT_LABELS } from '../constants/roles'

const store = useFactoriesStore()
const auth = useAuthStore()

onMounted(() => store.fetchAll())

const visible = computed(() =>
  filterByCraft(store.items, auth.role ? visibleCraft(auth.role) : null),
)
const statusLabel: Record<string, string> = {
  active: '正常', limited: '限单', suspended: '暂停', eliminated: '淘汰',
}
</script>
<template>
  <AppLayout>
    <h2>工厂列表（{{ visible.length }}）</h2>
    <RouterLink to="/factories/new">+ 新增工厂</RouterLink>
    <table>
      <thead><tr><th>名称</th><th>工艺</th><th>状态</th><th></th></tr></thead>
      <tbody>
        <tr v-for="f in visible" :key="f.id">
          <td>{{ f.name }}</td>
          <td>{{ CRAFT_LABELS[f.craft] }}</td>
          <td>{{ statusLabel[f.status] }}</td>
          <td><RouterLink :to="`/factories/${f.id}`">详情</RouterLink></td>
        </tr>
      </tbody>
    </table>
  </AppLayout>
</template>
```

- [ ] **Step 2: 手动验证**

后台手建 2 家不同工艺工厂。用 sc_manager 登录看到全部；用 buyer_painting 登录只看到喷油工厂。

- [ ] **Step 3: Commit**

```bash
git add src/views/FactoryListView.vue
git commit -m "feat(fe): add factory list view with role-based craft filtering"
```

---

### Task 3.3: 工厂新增/编辑表单（资质字段按权限锁定）

**Files:**
- Create: `src/components/FactoryForm.vue`
- Modify: `src/router/index.ts`（加 `/factories/new` 路由）
- Create: `src/views/FactoryDetailView.vue`

- [ ] **Step 1: 加 new 路由**

在 `src/router/index.ts` 的 routes 中，`/factories/:id` 之前加入：
```ts
  { path: '/factories/new', component: () => import('../views/FactoryDetailView.vue') },
```

- [ ] **Step 2: 写工厂表单组件**

Create `src/components/FactoryForm.vue`:
```vue
<script setup lang="ts">
import { reactive, computed } from 'vue'
import type { Factory } from '../types/factory'
import { useAuthStore } from '../stores/auth'
import { CRAFT_LABELS } from '../constants/roles'

const props = defineProps<{ modelValue: Partial<Factory> }>()
const emit = defineEmits<{ (e: 'save', v: Partial<Factory>): void }>()
const auth = useAuthStore()

const form = reactive<Partial<Factory>>({ status: 'active', ...props.modelValue })
// 资质有效期/资质文件：仅 admin、供应链经理可改
const canEditQual = computed(() => auth.role === 'admin' || auth.role === 'sc_manager')
</script>
<template>
  <form @submit.prevent="emit('save', { ...form })">
    <label>名称 <input v-model="form.name" required /></label>
    <label>工艺
      <select v-model="form.craft" required>
        <option v-for="(label, key) in CRAFT_LABELS" :key="key" :value="key">{{ label }}</option>
      </select>
    </label>
    <label>联系人 <input v-model="form.contact_person" /></label>
    <label>电话 <input v-model="form.contact_phone" /></label>
    <label>地址 <input v-model="form.address" /></label>
    <label>厂房面积(㎡) <input v-model.number="form.workshop_area" type="number" /></label>
    <label>资质有效期
      <input v-model="form.qualification_expiry" type="date" :disabled="!canEditQual" />
    </label>
    <p v-if="!canEditQual" class="hint">资质信息仅供应链经理/管理员可修改</p>
    <button type="submit">保存</button>
  </form>
</template>
```

- [ ] **Step 3: 写详情/编辑视图（new 与 edit 复用）**

Create `src/views/FactoryDetailView.vue`:
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import AppLayout from '../components/AppLayout.vue'
import FactoryForm from '../components/FactoryForm.vue'
import { useFactoriesStore } from '../stores/factories'
import { useAuthStore } from '../stores/auth'
import type { Factory } from '../types/factory'

const route = useRoute()
const router = useRouter()
const store = useFactoriesStore()
const auth = useAuthStore()

const isNew = route.path.endsWith('/new')
const factory = ref<Partial<Factory>>({})

onMounted(async () => {
  if (!isNew) factory.value = await store.get(route.params.id as string)
})

async function onSave(data: Partial<Factory>) {
  if (isNew) {
    const created = await store.create({ ...data, created_by: auth.userId ?? undefined })
    router.push(`/factories/${created.id}`)
  } else {
    await store.update(route.params.id as string, data)
    factory.value = { ...factory.value, ...data }
  }
}
</script>
<template>
  <AppLayout>
    <h2>{{ isNew ? '新增工厂' : factory.name }}</h2>
    <FactoryForm :model-value="factory" @save="onSave" />
  </AppLayout>
</template>
```

- [ ] **Step 4: 手动验证**

用 buyer 登录新增一家本工艺工厂，资质有效期输入框为禁用态；用 sc_manager 编辑同一家，资质有效期可改。保存后列表出现该厂。

- [ ] **Step 5: Commit**

```bash
git add src/components/FactoryForm.vue src/views/FactoryDetailView.vue src/router/index.ts
git commit -m "feat(fe): add factory create/edit form with qualification field lock"
```

---

### Task 3.4: 合作状态提报/审批（钩子强制）

**Files:**
- Create: `pb_hooks/factory_status_guard.pb.js`
- Modify: `src/views/FactoryDetailView.vue`（加状态提报/审批 UI）

- [ ] **Step 1: 写服务端钩子（非经理改 status 时拦截，只允许写 status_pending）**

Create `pb_hooks/factory_status_guard.pb.js`:
```js
// pb_hooks/factory_status_guard.pb.js
// 合作状态(status)只有 sc_manager/admin 能直接改；其他角色只能提报到 status_pending。
onRecordUpdateRequest((e) => {
  const auth = e.requestInfo().auth
  const role = auth ? auth.get('role') : ''
  if (role === 'admin' || role === 'sc_manager') {
    return e.next() // 经理审批：放行，并清空 pending
  }

  // 取数据库中的原值
  const original = $app.findRecordById('factories', e.record.id)
  const oldStatus = original.get('status')
  const newStatus = e.record.get('status')

  if (oldStatus !== newStatus) {
    // 非经理擅自改 status：回退，并把意图写入 status_pending
    e.record.set('status', oldStatus)
    e.record.set('status_pending', newStatus)
  }
  return e.next()
}, 'factories')
```

- [ ] **Step 2: 重启 PB 并手动验证钩子**

重启 `bin/pocketbase serve`。用 buyer 账号通过应用尝试把某厂状态改为"暂停"，保存后该厂 status 仍为原值、status_pending 记录为"暂停"。

- [ ] **Step 3: 详情页加状态提报/审批 UI**

在 `src/views/FactoryDetailView.vue` 的 `<FactoryForm .../>` 之后插入：
```vue
    <section v-if="!isNew" class="status-box">
      <h3>合作状态</h3>
      <p>当前：{{ factory.status }}　待审批：{{ factory.status_pending || '无' }}</p>
      <div v-if="auth.role && (auth.role === 'sc_manager' || auth.role === 'admin')">
        <label>审批为
          <select v-model="newStatus">
            <option value="active">正常</option>
            <option value="limited">限单</option>
            <option value="suspended">暂停</option>
            <option value="eliminated">淘汰</option>
          </select>
        </label>
        <button @click="approveStatus">确认审批</button>
      </div>
      <div v-else>
        <label>提报变更
          <select v-model="newStatus">
            <option value="active">正常</option>
            <option value="limited">限单</option>
            <option value="suspended">暂停</option>
            <option value="eliminated">淘汰</option>
          </select>
        </label>
        <button @click="proposeStatus">提报</button>
      </div>
    </section>
```
并在 `<script setup>` 中追加：
```ts
const newStatus = ref<Factory['status']>('active')
async function proposeStatus() {
  await store.update(route.params.id as string, { status: newStatus.value })
  factory.value = await store.get(route.params.id as string)
}
async function approveStatus() {
  await store.update(route.params.id as string, {
    status: newStatus.value, status_pending: '',
    status_updated_by: auth.userId ?? undefined,
    status_updated_at: new Date().toISOString(),
  })
  factory.value = await store.get(route.params.id as string)
}
```

- [ ] **Step 4: 手动验证完整流程**

buyer 提报"暂停"→ 详情显示"待审批：暂停"且当前状态不变；sc_manager 登录同厂审批"暂停"→ 当前状态变暂停、待审批清空。

- [ ] **Step 5: Commit**

```bash
git add pb_hooks/factory_status_guard.pb.js src/views/FactoryDetailView.vue
git commit -m "feat: enforce factory status approval flow (propose vs approve) via hook + UI"
```

---

## Phase 4 — 月度产值（财务专属）

### Task 4.1: 产值锁权钩子

**Files:**
- Create: `pb_hooks/monthly_output_guard.pb.js`

- [ ] **Step 1: 写钩子（仅 finance_cost/admin 可增改产值）**

Create `pb_hooks/monthly_output_guard.pb.js`:
```js
// pb_hooks/monthly_output_guard.pb.js
// 月度产值仅 finance_cost / admin 可写（创建+更新）。其他角色一律拒绝。
function assertFinance(e) {
  const auth = e.requestInfo().auth
  const role = auth ? auth.get('role') : ''
  if (role !== 'finance_cost' && role !== 'admin') {
    throw new ApiError(403, '仅财务成本会计可录入/修改加工产值')
  }
  return e.next()
}
onRecordCreateRequest((e) => assertFinance(e), 'monthly_output')
onRecordUpdateRequest((e) => assertFinance(e), 'monthly_output')
```

- [ ] **Step 2: 重启并手动验证**

重启 PB。用 buyer 通过 SDK/界面尝试创建 monthly_output → 收到 403；用 finance_cost → 成功。

- [ ] **Step 3: Commit**

```bash
git add pb_hooks/monthly_output_guard.pb.js
git commit -m "feat: enforce finance-only write on monthly_output via hook"
```

---

### Task 4.2: output store + 产值录入页

**Files:**
- Create: `src/stores/output.ts`, `src/views/MonthlyOutputView.vue`

- [ ] **Step 1: 写 output store**

Create `src/stores/output.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { MonthlyOutput } from '../types/output'

export const useOutputStore = defineStore('output', () => {
  const items = ref<MonthlyOutput[]>([])

  async function fetchByMonth(yearMonth: string) {
    items.value = await pb.collection('monthly_output').getFullList<MonthlyOutput>({
      filter: `year_month = "${yearMonth}"`,
      expand: 'factory',
      sort: 'factory',
    })
  }
  async function upsert(data: Partial<MonthlyOutput>) {
    // 依赖唯一索引 (factory, year_month)：存在则更新，否则创建
    const existing = data.factory && data.year_month
      ? await pb.collection('monthly_output').getFullList<MonthlyOutput>({
          filter: `factory = "${data.factory}" && year_month = "${data.year_month}"`,
        })
      : []
    if (existing.length) return pb.collection('monthly_output').update(existing[0].id, data)
    return pb.collection('monthly_output').create(data)
  }
  return { items, fetchByMonth, upsert }
})
```

- [ ] **Step 2: 写产值录入页**

Create `src/views/MonthlyOutputView.vue`:
```vue
<script setup lang="ts">
import { ref } from 'vue'
import AppLayout from '../components/AppLayout.vue'
import { useFactoriesStore } from '../stores/factories'
import { useOutputStore } from '../stores/output'
import { useAuthStore } from '../stores/auth'

const month = ref(new Date().toISOString().slice(0, 7))
const factories = useFactoriesStore()
const output = useOutputStore()
const auth = useAuthStore()
const drafts = ref<Record<string, { monthly_amount?: number; source_doc?: string }>>({})

async function load() {
  await factories.fetchAll()
  await output.fetchByMonth(month.value)
}

async function save(factoryId: string) {
  const d = drafts.value[factoryId] || {}
  await output.upsert({
    factory: factoryId,
    year_month: month.value,
    monthly_amount: d.monthly_amount,
    source_doc: d.source_doc,
    entered_by: auth.userId ?? undefined,
    entered_at: new Date().toISOString(),
  })
  await output.fetchByMonth(month.value)
}
</script>
<template>
  <AppLayout>
    <h2>月度产值录入</h2>
    <label>月份 <input v-model="month" type="month" @change="load" /></label>
    <button @click="load">加载</button>
    <table>
      <thead><tr><th>工厂</th><th>当月产值</th><th>对账单号</th><th></th></tr></thead>
      <tbody>
        <tr v-for="f in factories.items" :key="f.id">
          <td>{{ f.name }}</td>
          <td><input type="number" v-model.number="(drafts[f.id] ??= {}).monthly_amount" /></td>
          <td><input v-model="(drafts[f.id] ??= {}).source_doc" /></td>
          <td><button @click="save(f.id)">保存</button></td>
        </tr>
      </tbody>
    </table>
  </AppLayout>
</template>
```

- [ ] **Step 3: 手动验证**

finance_cost 登录 → 产值录入页 → 选月份加载 → 给某厂填金额保存 → 重新加载金额留存。buyer 账号导航无"产值录入"入口，直接访问 URL 也会因 403 失败。

- [ ] **Step 4: Commit**

```bash
git add src/stores/output.ts src/views/MonthlyOutputView.vue
git commit -m "feat(fe): add monthly output store and finance entry view"
```

---

## Phase 5 — 异常/事故记录

### Task 5.1: incidents store + 录入（含附件）

**Files:**
- Create: `src/stores/incidents.ts`
- Modify: `src/views/FactoryDetailView.vue`（详情页加异常列表+录入）

- [ ] **Step 1: 写 incidents store**

Create `src/stores/incidents.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { Incident } from '../types/incident'

export const useIncidentsStore = defineStore('incidents', () => {
  const items = ref<Incident[]>([])

  async function fetchByFactory(factoryId: string) {
    items.value = await pb.collection('incidents').getFullList<Incident>({
      filter: `factory = "${factoryId}"`,
      sort: '-incident_date',
    })
  }
  // data 用 FormData 以支持文件上传
  async function create(data: FormData) {
    return pb.collection('incidents').create(data)
  }
  return { items, fetchByFactory, create }
})
```

- [ ] **Step 2: 详情页加异常区块**

在 `src/views/FactoryDetailView.vue` 状态区块之后插入：
```vue
    <section v-if="!isNew" class="incidents">
      <h3>异常 / 事故记录</h3>
      <form @submit.prevent="submitIncident">
        <input v-model="inc.incident_date" type="date" required />
        <select v-model="inc.incident_type" required>
          <option value="batch_defect">批量不良</option>
          <option value="env_violation">环保违规</option>
          <option value="shutdown">停工</option>
          <option value="other">其他</option>
        </select>
        <input v-model="inc.description" placeholder="描述" />
        <input ref="photoInput" type="file" multiple accept="image/*" />
        <button type="submit">登记异常</button>
      </form>
      <ul>
        <li v-for="i in incidents.items" :key="i.id">
          {{ i.incident_date }} - {{ i.incident_type }} - {{ i.status }}
        </li>
      </ul>
    </section>
```
在 `<script setup>` 追加：
```ts
import { useIncidentsStore } from '../stores/incidents'
const incidents = useIncidentsStore()
const photoInput = ref<HTMLInputElement | null>(null)
const inc = ref<{ incident_date: string; incident_type: string; description: string }>({
  incident_date: '', incident_type: 'batch_defect', description: '',
})
async function submitIncident() {
  const fd = new FormData()
  fd.append('factory', route.params.id as string)
  fd.append('incident_date', inc.value.incident_date)
  fd.append('incident_type', inc.value.incident_type)
  fd.append('description', inc.value.description)
  fd.append('status', 'open')
  fd.append('entered_by', auth.userId ?? '')
  const files = photoInput.value?.files
  if (files) for (const f of Array.from(files)) fd.append('photos', f)
  await incidents.create(fd)
  await incidents.fetchByFactory(route.params.id as string)
}
```
并在 onMounted 的非 new 分支追加：`await incidents.fetchByFactory(route.params.id as string)`

- [ ] **Step 3: 手动验证**

品质/采购账号在工厂详情登记一条带图片的异常 → 列表出现该记录，后台 incidents 集合可见照片附件。

- [ ] **Step 4: Commit**

```bash
git add src/stores/incidents.ts src/views/FactoryDetailView.vue
git commit -m "feat(fe): add incident logging with photo attachments on factory detail"
```

---

## Phase 6 — 评分模板配置 + 月度评分引擎（表2）

### Task 6.1: scoreTemplates store + admin 配置页

**Files:**
- Create: `src/stores/scoreTemplates.ts`, `src/views/admin/ScoreTemplateAdminView.vue`

- [ ] **Step 1: 写 store**

Create `src/stores/scoreTemplates.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { ScoreTemplate } from '../types/score'

export const useScoreTemplatesStore = defineStore('scoreTemplates', () => {
  const items = ref<ScoreTemplate[]>([])

  async function fetchAll() {
    items.value = await pb.collection('score_templates').getFullList<ScoreTemplate>({
      sort: 'sort_order',
    })
  }
  // 取适用某工艺的启用项：通用项(craft_filter 空) + 该工艺专项
  function applicable(craft: string): ScoreTemplate[] {
    return items.value.filter(
      (t) => t.is_active && (!t.craft_filter || t.craft_filter === craft),
    )
  }
  async function create(data: Partial<ScoreTemplate>) {
    return pb.collection('score_templates').create(data)
  }
  async function update(id: string, data: Partial<ScoreTemplate>) {
    return pb.collection('score_templates').update(id, data)
  }
  async function remove(id: string) {
    return pb.collection('score_templates').delete(id)
  }
  return { items, fetchAll, applicable, create, update, remove }
})
```

- [ ] **Step 2: 写 admin 配置页**

Create `src/views/admin/ScoreTemplateAdminView.vue`:
```vue
<script setup lang="ts">
import { onMounted, ref, computed } from 'vue'
import AppLayout from '../../components/AppLayout.vue'
import { useScoreTemplatesStore } from '../../stores/scoreTemplates'
import type { ScoreTemplate } from '../../types/score'

const store = useScoreTemplatesStore()
const draft = ref<Partial<ScoreTemplate>>({
  module: 'craft_specific', scoring_role: 'quality_qc', is_active: true, max_score: 0, sort_order: 99,
})
onMounted(() => store.fetchAll())
const total = computed(() => store.items.filter((t) => t.is_active).reduce((s, t) => s + t.max_score, 0))

async function add() {
  await store.create({ ...draft.value })
  await store.fetchAll()
}
async function toggle(t: ScoreTemplate) {
  await store.update(t.id, { is_active: !t.is_active })
  await store.fetchAll()
}
</script>
<template>
  <AppLayout>
    <h2>评分模板配置（当前启用合计 {{ total }} 分）</h2>
    <p v-if="total !== 100" class="hint">提示：通用70+专项30 应为100分，当前 {{ total }} 分</p>
    <table>
      <thead><tr><th>名称</th><th>模块</th><th>分值</th><th>打分主体</th><th>工艺</th><th>启用</th></tr></thead>
      <tbody>
        <tr v-for="t in store.items" :key="t.id">
          <td>{{ t.name }}</td><td>{{ t.module }}</td><td>{{ t.max_score }}</td>
          <td>{{ t.scoring_role }}</td><td>{{ t.craft_filter || '通用' }}</td>
          <td><button @click="toggle(t)">{{ t.is_active ? '停用' : '启用' }}</button></td>
        </tr>
      </tbody>
    </table>
    <h3>新增评分项</h3>
    <form @submit.prevent="add">
      <input v-model="draft.name" placeholder="名称" required />
      <input v-model.number="draft.max_score" type="number" placeholder="分值" required />
      <select v-model="draft.scoring_role">
        <option value="buyer">采购</option>
        <option value="quality_qc">品质QC</option>
      </select>
      <select v-model="draft.craft_filter">
        <option value="">通用</option>
        <option value="injection">注塑</option>
        <option value="painting">喷油</option>
        <option value="assembly">装配</option>
        <option value="sewing">毛绒车缝</option>
      </select>
      <button type="submit">添加</button>
    </form>
  </AppLayout>
</template>
```

- [ ] **Step 3: 手动验证**

admin 登录 → 评分模板页看到 6 条默认项，合计提示 70 分；新增一条 30 分的注塑专项后，注塑工艺合计达 100。非 admin 访问 `/admin/score-templates` 被守卫弹回首页。

- [ ] **Step 4: Commit**

```bash
git add src/stores/scoreTemplates.ts src/views/admin/ScoreTemplateAdminView.vue
git commit -m "feat(fe): add score template store and admin config view"
```

---

### Task 6.2: 评分自动算分 + 评级钩子

**Files:**
- Create: `pb_hooks/score_calc.pb.js`

- [ ] **Step 1: 写钩子（创建/更新评分时，服务端按 score_items 求和算总分+评级）**

Create `pb_hooks/score_calc.pb.js`:
```js
// pb_hooks/score_calc.pb.js
// 服务端权威算分：total_score = sum(score_items[].score)，并据评级线写 grade。
// 防止前端篡改总分；与前端 utils/grading.ts 规则保持一致。
function recalc(e) {
  const itemsRaw = e.record.get('score_items')
  let items = []
  try {
    items = typeof itemsRaw === 'string' ? JSON.parse(itemsRaw) : (itemsRaw || [])
  } catch (_) {
    items = []
  }
  let total = 0
  for (const it of items) total += Number(it.score) || 0

  let grade = 'D'
  if (total >= 90) grade = 'A'
  else if (total >= 80) grade = 'B'
  else if (total >= 70) grade = 'C'

  e.record.set('total_score', total)
  e.record.set('grade', grade)
  return e.next()
}
onRecordCreateRequest((e) => recalc(e), 'monthly_scores')
onRecordUpdateRequest((e) => recalc(e), 'monthly_scores')
```

- [ ] **Step 2: 重启并手动验证**

重启 PB。在后台 monthly_scores 手建一条记录，score_items 填 `[{"template_id":"x","score":85}]` 并保存 → total_score 自动变 85、grade 自动变 B（即便提交时手填别的总分也被覆盖）。

- [ ] **Step 3: Commit**

```bash
git add pb_hooks/score_calc.pb.js
git commit -m "feat: server-authoritative score total + grade calculation hook"
```

---

### Task 6.3: scores store + 月度评分单页

**Files:**
- Create: `src/stores/scores.ts`, `src/views/ScoreSheetView.vue`

- [ ] **Step 1: 写 scores store**

Create `src/stores/scores.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { MonthlyScore } from '../types/score'

export const useScoresStore = defineStore('scores', () => {
  const items = ref<MonthlyScore[]>([])

  async function fetchByMonth(yearMonth: string) {
    items.value = await pb.collection('monthly_scores').getFullList<MonthlyScore>({
      filter: `year_month = "${yearMonth}"`,
      expand: 'factory',
    })
  }
  async function getOne(factoryId: string, yearMonth: string): Promise<MonthlyScore | null> {
    const r = await pb.collection('monthly_scores').getFullList<MonthlyScore>({
      filter: `factory = "${factoryId}" && year_month = "${yearMonth}"`,
    })
    return r[0] ?? null
  }
  async function save(factoryId: string, yearMonth: string, data: Partial<MonthlyScore>) {
    const existing = await getOne(factoryId, yearMonth)
    if (existing) return pb.collection('monthly_scores').update(existing.id, data)
    return pb.collection('monthly_scores').create({
      factory: factoryId, year_month: yearMonth, status: 'draft', flag: 'none', ...data,
    })
  }
  return { items, fetchByMonth, getOne, save }
})
```

- [ ] **Step 2: 写评分单页（采购填采购项、品质填品质项，前端只读显示总分）**

Create `src/views/ScoreSheetView.vue`:
```vue
<script setup lang="ts">
import { ref, onMounted, computed } from 'vue'
import { useRoute } from 'vue-router'
import AppLayout from '../components/AppLayout.vue'
import { useScoresStore } from '../stores/scores'
import { useScoreTemplatesStore } from '../stores/scoreTemplates'
import { useFactoriesStore } from '../stores/factories'
import { useAuthStore } from '../stores/auth'
import { totalFromItems, gradeFromScore } from '../utils/grading'
import { isBuyer } from '../constants/roles'
import type { ScoreItem } from '../types/score'

const route = useRoute()
const scores = useScoresStore()
const templates = useScoreTemplatesStore()
const factories = useFactoriesStore()
const auth = useAuthStore()

const factoryId = route.params.id as string
const month = route.params.month as string
const craft = ref<string>('')
const itemMap = ref<Record<string, ScoreItem>>({})

onMounted(async () => {
  await templates.fetchAll()
  const f = await factories.get(factoryId)
  craft.value = f.craft
  const existing = await scores.getOne(factoryId, month)
  if (existing?.score_items) for (const it of existing.score_items) itemMap.value[it.template_id] = it
})

const applicable = computed(() => templates.applicable(craft.value))
// 当前角色能编辑的评分项：采购角色编辑 scoring_role=buyer，品质编辑 quality_qc
const canEditItem = (scoringRole: string) =>
  (scoringRole === 'buyer' && auth.role && isBuyer(auth.role)) ||
  (scoringRole === 'quality_qc' && auth.role === 'quality_qc') ||
  auth.role === 'admin'

const liveTotal = computed(() =>
  totalFromItems(applicable.value.map((t) => itemMap.value[t.id] ?? { template_id: t.id, score: 0 })),
)
const liveGrade = computed(() => gradeFromScore(liveTotal.value))

async function submit() {
  const score_items = applicable.value.map((t) => ({
    template_id: t.id,
    score: itemMap.value[t.id]?.score ?? 0,
    notes: itemMap.value[t.id]?.notes ?? '',
  }))
  await scores.save(factoryId, month, { score_items, status: 'submitted', submitted_by: auth.userId ?? undefined })
  alert('已提交，总分由服务端核定')
}
</script>
<template>
  <AppLayout>
    <h2>评分单 — {{ month }}</h2>
    <table>
      <thead><tr><th>评分项</th><th>满分</th><th>得分</th><th>主体</th></tr></thead>
      <tbody>
        <tr v-for="t in applicable" :key="t.id">
          <td>{{ t.name }}</td>
          <td>{{ t.max_score }}</td>
          <td>
            <input type="number" :max="t.max_score" min="0"
              :disabled="!canEditItem(t.scoring_role)"
              v-model.number="(itemMap[t.id] ??= { template_id: t.id, score: 0 }).score" />
          </td>
          <td>{{ t.scoring_role === 'buyer' ? '采购' : '品质' }}</td>
        </tr>
      </tbody>
    </table>
    <p><strong>预估总分：{{ liveTotal }}（{{ liveGrade }}级）</strong> — 最终以服务端核定为准</p>
    <button @click="submit">提交评分</button>
  </AppLayout>
</template>
```

- [ ] **Step 3: 手动验证**

buyer 进 `/factories/<id>/score/2026-05`，只能改采购项，品质项禁用；填分后预估总分实时更新；提交后后台记录 total_score/grade 由钩子核定。品质账号补填品质项后总分完整。

- [ ] **Step 4: Commit**

```bash
git add src/stores/scores.ts src/views/ScoreSheetView.vue
git commit -m "feat(fe): add monthly score sheet with per-role item editing and live total"
```

---

### Task 6.4: 红黄牌审批（经理终审）

**Files:**
- Create: `pb_hooks/flag_guard.pb.js`
- Modify: `src/views/ScoreSheetView.vue`（加红黄牌区块）

- [ ] **Step 1: 写钩子（flag 字段变更仅经理/admin 可生效；其他角色只能写依据 flag_reason）**

Create `pb_hooks/flag_guard.pb.js`:
```js
// pb_hooks/flag_guard.pb.js
// 红黄牌(flag)的最终生效仅 sc_manager/admin；品质QC 可写 flag_reason 出依据但不能定级。
onRecordUpdateRequest((e) => {
  const auth = e.requestInfo().auth
  const role = auth ? auth.get('role') : ''
  if (role === 'sc_manager' || role === 'admin') return e.next()

  const original = $app.findRecordById('monthly_scores', e.record.id)
  const oldFlag = original.get('flag')
  const newFlag = e.record.get('flag')
  if (oldFlag !== newFlag) {
    e.record.set('flag', oldFlag) // 非经理不得改 flag，回退
  }
  return e.next()
}, 'monthly_scores')
```

- [ ] **Step 2: 重启并验证钩子**

重启 PB。品质账号尝试把某评分单 flag 改 red → 保存后 flag 未变；经理改 red → 生效。

- [ ] **Step 3: 评分单页加红黄牌区块**

在 `src/views/ScoreSheetView.vue` 的提交按钮之后插入：
```vue
    <section class="flag-box">
      <h3>红黄牌</h3>
      <label>问题依据 <textarea v-model="flagReason"></textarea></label>
      <div v-if="auth.role === 'sc_manager' || auth.role === 'admin'">
        <select v-model="flag">
          <option value="none">无</option>
          <option value="yellow">黄牌</option>
          <option value="red">红牌</option>
        </select>
        <button @click="saveFlag">经理终审</button>
      </div>
      <button v-else @click="saveReason">提交依据</button>
    </section>
```
`<script setup>` 追加：
```ts
const flag = ref<'none' | 'yellow' | 'red'>('none')
const flagReason = ref('')
async function saveReason() {
  await scores.save(factoryId, month, { flag_reason: flagReason.value, flag_issued_by: auth.userId ?? undefined })
  alert('依据已提交，等待经理终审')
}
async function saveFlag() {
  await scores.save(factoryId, month, {
    flag: flag.value, flag_reason: flagReason.value, flag_approved_by: auth.userId ?? undefined,
  })
  alert('红黄牌已终审')
}
```

- [ ] **Step 4: 手动验证**

品质提交红牌依据 → 经理看到依据并终审红牌 → 评分单 flag=red。

- [ ] **Step 5: Commit**

```bash
git add pb_hooks/flag_guard.pb.js src/views/ScoreSheetView.vue
git commit -m "feat: red/yellow flag approval (quality issues evidence, manager final approval)"
```

---

## Phase 7 — 月度评审大盘 + 评审会议（表3）

### Task 7.1: 大盘汇总计算（纯函数 + 测试）

**Files:**
- Create: `src/utils/summary.ts`
- Test: `tests/summary.spec.ts`

- [ ] **Step 1: 写失败测试**

Create `tests/summary.spec.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { summarizeByCraft } from '../src/utils/summary'

describe('summarizeByCraft', () => {
  it('aggregates count, grade dist, avg score, total output per craft', () => {
    const factories = [
      { id: 'f1', craft: 'injection' }, { id: 'f2', craft: 'injection' },
    ] as any[]
    const scores = [
      { factory: 'f1', total_score: 90, grade: 'A' },
      { factory: 'f2', total_score: 70, grade: 'C' },
    ] as any[]
    const outputs = [
      { factory: 'f1', monthly_amount: 1000 },
      { factory: 'f2', monthly_amount: 500 },
    ] as any[]
    const r = summarizeByCraft(factories, scores, outputs)
    expect(r.injection.factory_count).toBe(2)
    expect(r.injection.grade_dist).toEqual({ A: 1, B: 0, C: 1, D: 0 })
    expect(r.injection.avg_score).toBe(80)
    expect(r.injection.total_output).toBe(1500)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- summary`
Expected: FAIL

- [ ] **Step 3: 写实现**

Create `src/utils/summary.ts`:
```ts
import type { Factory } from '../types/factory'
import type { MonthlyScore } from '../types/score'
import type { MonthlyOutput } from '../types/output'
import type { CraftSummary } from '../types/review'
import type { Craft } from '../constants/roles'

export function summarizeByCraft(
  factories: Factory[], scores: MonthlyScore[], outputs: MonthlyOutput[],
): Record<Craft, CraftSummary> {
  const scoreByFactory = new Map(scores.map((s) => [s.factory, s]))
  const outputByFactory = new Map(outputs.map((o) => [o.factory, o]))
  const result = {} as Record<Craft, CraftSummary>

  for (const f of factories) {
    const c = f.craft
    if (!result[c]) {
      result[c] = { factory_count: 0, grade_dist: { A: 0, B: 0, C: 0, D: 0 }, avg_score: 0, total_output: 0 }
    }
    const bucket = result[c]
    bucket.factory_count++
    const s = scoreByFactory.get(f.id)
    if (s?.grade) bucket.grade_dist[s.grade]++
    bucket.total_output += outputByFactory.get(f.id)?.monthly_amount ?? 0
    bucket.avg_score += s?.total_score ?? 0
  }
  for (const c of Object.keys(result) as Craft[]) {
    const b = result[c]
    b.avg_score = b.factory_count ? Math.round((b.avg_score / b.factory_count) * 100) / 100 : 0
  }
  return result
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- summary`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/utils/summary.ts tests/summary.spec.ts
git commit -m "feat(fe): add per-craft summary aggregation util with tests"
```

---

### Task 7.2: reviews store + 大盘页 + 评审会议页

**Files:**
- Create: `src/stores/reviews.ts`, `src/views/ReviewBoardView.vue`, `src/views/ReviewMeetingView.vue`

- [ ] **Step 1: 写 reviews store**

Create `src/stores/reviews.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { ReviewMeeting } from '../types/review'

export const useReviewsStore = defineStore('reviews', () => {
  const current = ref<ReviewMeeting | null>(null)

  async function fetchByMonth(yearMonth: string) {
    const r = await pb.collection('review_meetings').getFullList<ReviewMeeting>({
      filter: `year_month = "${yearMonth}"`,
    })
    current.value = r[0] ?? null
    return current.value
  }
  async function save(yearMonth: string, data: Partial<ReviewMeeting>) {
    const existing = await fetchByMonth(yearMonth)
    if (existing) return pb.collection('review_meetings').update(existing.id, data)
    return pb.collection('review_meetings').create({ year_month: yearMonth, status: 'draft', ...data })
  }
  return { current, fetchByMonth, save }
})
```

- [ ] **Step 2: 写大盘页**

Create `src/views/ReviewBoardView.vue`:
```vue
<script setup lang="ts">
import { ref, onMounted, computed } from 'vue'
import { useRoute, RouterLink } from 'vue-router'
import AppLayout from '../components/AppLayout.vue'
import { useFactoriesStore } from '../stores/factories'
import { useScoresStore } from '../stores/scores'
import { useOutputStore } from '../stores/output'
import { useReviewsStore } from '../stores/reviews'
import { summarizeByCraft } from '../utils/summary'
import { CRAFT_LABELS } from '../constants/roles'
import { useAuthStore } from '../stores/auth'

const route = useRoute()
const month = route.params.month as string
const factories = useFactoriesStore()
const scores = useScoresStore()
const output = useOutputStore()
const reviews = useReviewsStore()
const auth = useAuthStore()

onMounted(async () => {
  await Promise.all([factories.fetchAll(), scores.fetchByMonth(month), output.fetchByMonth(month)])
})
const summary = computed(() => summarizeByCraft(factories.items, scores.items, output.items))

async function saveSummary() {
  await reviews.save(month, { summary_by_craft: summary.value })
  alert('大盘已存档')
}
</script>
<template>
  <AppLayout>
    <h2>月度评审大盘 — {{ month }}</h2>
    <table>
      <thead><tr><th>品类</th><th>工厂数</th><th>A</th><th>B</th><th>C</th><th>D</th><th>平均分</th><th>总产值</th></tr></thead>
      <tbody>
        <tr v-for="(s, craft) in summary" :key="craft">
          <td>{{ CRAFT_LABELS[craft] }}</td>
          <td>{{ s.factory_count }}</td>
          <td>{{ s.grade_dist.A }}</td><td>{{ s.grade_dist.B }}</td>
          <td>{{ s.grade_dist.C }}</td><td>{{ s.grade_dist.D }}</td>
          <td>{{ s.avg_score }}</td><td>{{ s.total_output }}</td>
        </tr>
      </tbody>
    </table>
    <button v-if="auth.role === 'sc_clerk' || auth.role === 'admin'" @click="saveSummary">存档大盘</button>
    <RouterLink :to="`/review/${month}/meeting`">→ 评审会议记录</RouterLink>
  </AppLayout>
</template>
```

- [ ] **Step 3: 写评审会议页**

Create `src/views/ReviewMeetingView.vue`:
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import AppLayout from '../components/AppLayout.vue'
import { useReviewsStore } from '../stores/reviews'
import { useAuthStore } from '../stores/auth'
import { canApproveStatus } from '../utils/permissions'

const route = useRoute()
const month = route.params.month as string
const reviews = useReviewsStore()
const auth = useAuthStore()
const suggestions = ref('')

onMounted(async () => {
  const m = await reviews.fetchByMonth(month)
  if (m) suggestions.value = m.optimization_suggestions ?? ''
})
async function saveDraft() {
  await reviews.save(month, { optimization_suggestions: suggestions.value })
  alert('已保存')
}
async function approve() {
  await reviews.save(month, {
    optimization_suggestions: suggestions.value,
    status: 'approved', approved_by: auth.userId ?? undefined,
    meeting_date: new Date().toISOString(),
  })
  alert('评审已审批通过')
}
</script>
<template>
  <AppLayout>
    <h2>月度评审会议 — {{ month }}</h2>
    <label>优化 / 整合 / 淘汰建议
      <textarea v-model="suggestions" rows="6"></textarea>
    </label>
    <button @click="saveDraft">保存草稿</button>
    <button v-if="auth.role && canApproveStatus(auth.role)" @click="approve">审批通过</button>
    <p>状态：{{ reviews.current?.status ?? 'draft' }}</p>
  </AppLayout>
</template>
```

- [ ] **Step 4: 手动验证**

文员进 `/review/2026-05` 看到各品类汇总并存档；经理进会议页填建议并审批通过；非经理无审批按钮。

- [ ] **Step 5: Commit**

```bash
git add src/stores/reviews.ts src/views/ReviewBoardView.vue src/views/ReviewMeetingView.vue
git commit -m "feat(fe): add review board (per-craft summary) and meeting approval views"
```

---

## Phase 8 — 岗位 KPI 自动统计

### Task 8.1: KPI 记录钩子（关键动作自动写 kpi_logs）

**Files:**
- Create: `pb_hooks/kpi_logger.pb.js`

- [ ] **Step 1: 写钩子（产值录入/评分提交/汇总存档时自动记 KPI + 按时判定）**

Create `pb_hooks/kpi_logger.pb.js`:
```js
// pb_hooks/kpi_logger.pb.js
// 在关键业务动作成功后，写一条 kpi_logs，并按 SLA 截止日判定是否按时。
// SLA（默认，可调）：产值=每月5日, 评分=每月4日, 汇总=每月6日（针对 target_month 的次月日期）。
function slaDeadline(targetMonth, dayOfNextMonth) {
  // targetMonth: "2026-05" → 次月该日 "2026-06-05"
  const [y, m] = targetMonth.split('-').map((x) => parseInt(x, 10))
  const ny = m === 12 ? y + 1 : y
  const nm = m === 12 ? 1 : m + 1
  const dd = String(dayOfNextMonth).padStart(2, '0')
  return `${ny}-${String(nm).padStart(2, '0')}-${dd}`
}

function logKpi(actionType, userId, targetMonth, slaDay, completedAtIso) {
  const col = $app.findCollectionByNameOrId('kpi_logs')
  const deadline = slaDeadline(targetMonth, slaDay)
  const onTime = completedAtIso.slice(0, 10) <= deadline
  const rec = new Record(col, {
    user: userId, action_type: actionType, target_month: targetMonth,
    deadline: deadline, completed_at: completedAtIso, is_on_time: onTime,
  })
  $app.save(rec)
}

// 产值录入（finance_cost） SLA 每月5日
onRecordAfterCreateSuccess((e) => {
  const uid = e.record.get('entered_by')
  const tm = e.record.get('year_month')
  if (uid && tm) logKpi('output_entered', uid, tm, 5, e.record.get('entered_at') || e.record.created)
}, 'monthly_output')

// 评分提交（submitted） SLA 每月4日
onRecordAfterUpdateSuccess((e) => {
  if (e.record.get('status') !== 'submitted') return
  const uid = e.record.get('submitted_by')
  const tm = e.record.get('year_month')
  if (uid && tm) logKpi('score_submitted', uid, tm, 4, e.record.updated)
}, 'monthly_scores')

// 汇总存档（review_meetings 有 summary_by_craft） SLA 每月6日
onRecordAfterUpdateSuccess((e) => {
  const summary = e.record.get('summary_by_craft')
  if (!summary) return
  const uid = e.requestInfo().auth ? e.requestInfo().auth.id : ''
  const tm = e.record.get('year_month')
  if (uid && tm) logKpi('summary_done', uid, tm, 6, e.record.updated)
}, 'review_meetings')
```

> 注：若某 `onRecordAfter*Success` 钩子名在当前 PB 版本不可用，用对应的 `onRecordAfterCreateRequest/onRecordAfterUpdateRequest`（在 `e.next()` 之后执行）替代——以 `bin/pocketbase serve` 启动时是否报"unknown hook"为准，先验证 Step 2。

- [ ] **Step 2: 重启验证钩子加载无误**

重启 `bin/pocketbase serve`，确认启动日志无"unknown handler/hook"报错。finance_cost 录一条产值 → kpi_logs 自动出现一条 output_entered，is_on_time 据当天日期判定。

- [ ] **Step 3: Commit**

```bash
git add pb_hooks/kpi_logger.pb.js
git commit -m "feat: auto-log position KPI with on-time SLA judgement via hooks"
```

---

### Task 8.2: KPI 聚合（纯函数 + 测试） + 看板页

**Files:**
- Create: `src/utils/kpi.ts`, `src/stores/kpi.ts`, `src/views/KpiBoardView.vue`
- Test: `tests/kpi.spec.ts`

- [ ] **Step 1: 写失败测试**

Create `tests/kpi.spec.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { onTimeRate } from '../src/utils/kpi'

describe('onTimeRate', () => {
  it('returns 0 for empty', () => expect(onTimeRate([])).toBe(0))
  it('computes percentage of on-time logs', () => {
    const logs = [
      { is_on_time: true }, { is_on_time: true }, { is_on_time: false }, { is_on_time: true },
    ] as any[]
    expect(onTimeRate(logs)).toBe(75)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- kpi`
Expected: FAIL

- [ ] **Step 3: 写聚合工具**

Create `src/utils/kpi.ts`:
```ts
import type { KpiLog } from '../types/kpi'

export function onTimeRate(logs: KpiLog[]): number {
  if (!logs.length) return 0
  const onTime = logs.filter((l) => l.is_on_time).length
  return Math.round((onTime / logs.length) * 100)
}

export function groupByUser(logs: KpiLog[]): Record<string, KpiLog[]> {
  const map: Record<string, KpiLog[]> = {}
  for (const l of logs) (map[l.user] ??= []).push(l)
  return map
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- kpi`
Expected: PASS

- [ ] **Step 5: 写 kpi store**

Create `src/stores/kpi.ts`:
```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { pb } from '../pb'
import type { KpiLog } from '../types/kpi'

export const useKpiStore = defineStore('kpi', () => {
  const items = ref<KpiLog[]>([])
  async function fetchByMonth(targetMonth: string) {
    items.value = await pb.collection('kpi_logs').getFullList<KpiLog>({
      filter: `target_month = "${targetMonth}"`,
      expand: 'user',
      sort: 'user',
    })
  }
  return { items, fetchByMonth }
})
```

- [ ] **Step 6: 写 KPI 看板页**

Create `src/views/KpiBoardView.vue`:
```vue
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import AppLayout from '../components/AppLayout.vue'
import { useKpiStore } from '../stores/kpi'
import { groupByUser, onTimeRate } from '../utils/kpi'

const month = ref(new Date().toISOString().slice(0, 7))
const kpi = useKpiStore()

onMounted(() => kpi.fetchByMonth(month.value))
const grouped = computed(() => groupByUser(kpi.items))

function nameOf(userId: string): string {
  const log = kpi.items.find((l) => l.user === userId) as any
  return log?.expand?.user?.display_name ?? userId
}
</script>
<template>
  <AppLayout>
    <h2>岗位 KPI 看板</h2>
    <label>月份 <input v-model="month" type="month" @change="kpi.fetchByMonth(month)" /></label>
    <table>
      <thead><tr><th>岗位/人员</th><th>履职次数</th><th>按时率</th></tr></thead>
      <tbody>
        <tr v-for="(logs, userId) in grouped" :key="userId">
          <td>{{ nameOf(userId) }}</td>
          <td>{{ logs.length }}</td>
          <td>{{ onTimeRate(logs) }}%</td>
        </tr>
      </tbody>
    </table>
  </AppLayout>
</template>
```

- [ ] **Step 7: 手动验证**

制造几条不同按时情况的动作（按时/逾期录产值），KPI 看板按人员显示履职次数与按时率。

- [ ] **Step 8: Commit**

```bash
git add src/utils/kpi.ts src/stores/kpi.ts src/views/KpiBoardView.vue tests/kpi.spec.ts
git commit -m "feat(fe): add KPI aggregation util, store, and position KPI board view"
```

---

### Task 8.3: 用户管理页（admin 建账号/设角色工艺）

**Files:**
- Create: `src/views/admin/UserAdminView.vue`

- [ ] **Step 1: 写用户管理页**

Create `src/views/admin/UserAdminView.vue`:
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import AppLayout from '../../components/AppLayout.vue'
import { pb } from '../../pb'
import { ROLE_LABELS, CRAFT_LABELS, type Role } from '../../constants/roles'

const users = ref<any[]>([])
const draft = ref({ email: '', password: '', display_name: '', role: 'buyer_injection' as Role, craft: '' })

async function load() {
  users.value = await pb.collection('users').getFullList({ sort: 'role' })
}
onMounted(load)

async function createUser() {
  await pb.collection('users').create({
    email: draft.value.email,
    password: draft.value.password,
    passwordConfirm: draft.value.password,
    display_name: draft.value.display_name,
    role: draft.value.role,
    craft: draft.value.craft || undefined,
  })
  await load()
}
</script>
<template>
  <AppLayout>
    <h2>用户管理</h2>
    <table>
      <thead><tr><th>姓名</th><th>邮箱</th><th>角色</th><th>工艺</th></tr></thead>
      <tbody>
        <tr v-for="u in users" :key="u.id">
          <td>{{ u.display_name }}</td><td>{{ u.email }}</td>
          <td>{{ ROLE_LABELS[u.role as Role] }}</td>
          <td>{{ u.craft ? CRAFT_LABELS[u.craft] : '-' }}</td>
        </tr>
      </tbody>
    </table>
    <h3>新建用户</h3>
    <form @submit.prevent="createUser">
      <input v-model="draft.display_name" placeholder="姓名" required />
      <input v-model="draft.email" type="email" placeholder="邮箱" required />
      <input v-model="draft.password" type="password" placeholder="密码(≥8位)" required />
      <select v-model="draft.role">
        <option v-for="(label, key) in ROLE_LABELS" :key="key" :value="key">{{ label }}</option>
      </select>
      <select v-model="draft.craft">
        <option value="">无工艺</option>
        <option v-for="(label, key) in CRAFT_LABELS" :key="key" :value="key">{{ label }}</option>
      </select>
      <button type="submit">创建</button>
    </form>
  </AppLayout>
</template>
```

- [ ] **Step 2: 手动验证**

admin 建一个 buyer_painting + craft=painting 的账号；该账号登录后工厂列表仅见喷油工厂。

- [ ] **Step 3: Commit**

```bash
git add src/views/admin/UserAdminView.vue
git commit -m "feat(fe): add admin user management view (create users with role/craft)"
```

---

## Phase 9 — 首页待办 + 全量回归 + 部署

### Task 9.1: 首页待办与本月进度

**Files:**
- Modify: `src/views/DashboardView.vue`

- [ ] **Step 1: 充实 Dashboard（按角色显示待办）**

Replace `src/views/DashboardView.vue`:
```vue
<script setup lang="ts">
import { ref, onMounted, computed } from 'vue'
import { RouterLink } from 'vue-router'
import AppLayout from '../components/AppLayout.vue'
import { useAuthStore } from '../stores/auth'
import { useScoresStore } from '../stores/scores'
import { canEditOutput } from '../utils/permissions'

const auth = useAuthStore()
const scores = useScoresStore()
const month = ref(new Date().toISOString().slice(0, 7))

onMounted(() => scores.fetchByMonth(month.value))
const submitted = computed(() => scores.items.filter((s) => s.status !== 'draft').length)
</script>
<template>
  <AppLayout>
    <h2>首页 — {{ month }}</h2>
    <p>欢迎，{{ auth.displayName }}。</p>
    <ul>
      <li>本月已提交评分单：{{ submitted }} 份</li>
      <li v-if="auth.role && canEditOutput(auth.role)">
        <RouterLink to="/monthly-output">→ 录入本月产值（每月5日前）</RouterLink>
      </li>
      <li><RouterLink :to="`/review/${month}`">→ 查看本月评审大盘</RouterLink></li>
      <li><RouterLink to="/kpi">→ 岗位 KPI 看板</RouterLink></li>
    </ul>
  </AppLayout>
</template>
```

- [ ] **Step 2: 手动验证**

不同角色登录首页看到对应待办；财务能看到产值录入入口，采购看不到。

- [ ] **Step 3: Commit**

```bash
git add src/views/DashboardView.vue
git commit -m "feat(fe): add role-aware dashboard with monthly todos and progress"
```

---

### Task 9.2: 全量测试 + 构建回归

- [ ] **Step 1: 跑全部单测**

Run: `npm test`
Expected: 全部 PASS（grading / permissions / auth / factories.store / summary / kpi）

- [ ] **Step 2: 类型检查 + 生产构建**

Run: `npx vue-tsc --noEmit && npm run build`
Expected: 无类型错误，`dist/` 生成成功

- [ ] **Step 3: 端到端冒烟（本地）**

`bin/pocketbase serve` + 预览 `npm run preview`，用各角色账号走一遍：登录→建厂→录产值→评分→红黄牌→大盘→评审审批→KPI看板，确认无控制台报错。

- [ ] **Step 4: Commit（若有修复）**

```bash
git add -A
git commit -m "test: full regression pass (unit + typecheck + build + smoke)"
```

---

### Task 9.3: 云端部署文档与脚本

**Files:**
- Create: `docs/DEPLOY.md`, `deploy/nginx.conf`, `deploy/backup.sh`

- [ ] **Step 1: 写 Nginx 配置**

Create `deploy/nginx.conf`:
```nginx
server {
  listen 80;
  server_name your-domain.com;        # 替换为实际域名

  # 前端静态文件
  root /opt/factory-review/dist;
  index index.html;
  location / {
    try_files $uri $uri/ /index.html; # SPA history 路由回退
  }

  # 反代 PocketBase（API + 文件 + 管理后台）
  location ~ ^/(api|_)/ {
    proxy_pass http://127.0.0.1:8090;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
```

- [ ] **Step 2: 写每日备份脚本**

Create `deploy/backup.sh`:
```bash
#!/usr/bin/env bash
# 每日备份 PocketBase 数据，保留最近 14 天。可选上传对象存储。
set -euo pipefail
BACKUP_DIR=/opt/factory-review/backups
DATA_DIR=/opt/factory-review/pb_data
STAMP=$(date +%Y%m%d_%H%M%S)
mkdir -p "$BACKUP_DIR"
tar -czf "$BACKUP_DIR/pb_$STAMP.tar.gz" -C "$(dirname "$DATA_DIR")" "$(basename "$DATA_DIR")"
# 仅保留最近14天
find "$BACKUP_DIR" -name 'pb_*.tar.gz' -mtime +14 -delete
# 可选：上传阿里云 OSS（需先装并配置 ossutil）
# ossutil cp "$BACKUP_DIR/pb_$STAMP.tar.gz" oss://your-bucket/factory-review/
echo "backup done: pb_$STAMP.tar.gz"
```

- [ ] **Step 3: 写部署文档**

Create `docs/DEPLOY.md`:
```markdown
# 云端部署（国内 VPS，2核4G）

## 1. 准备
- VPS（阿里云/腾讯云），Ubuntu 22.04，开放 80/443
- 域名解析到 VPS 公网 IP

## 2. 上传文件到 /opt/factory-review/
- `pocketbase`（linux_amd64 版本，从 GitHub releases 下载）
- `pb_migrations/`、`pb_hooks/`
- `dist/`（本地 `npm run build` 产物）

## 3. PocketBase 作为 systemd 服务
创建 /etc/systemd/system/factory-review.service：
\`\`\`
[Unit]
Description=Factory Review PocketBase
After=network.target
[Service]
Type=simple
WorkingDirectory=/opt/factory-review
ExecStart=/opt/factory-review/pocketbase serve --http=127.0.0.1:8090
Restart=always
[Install]
WantedBy=multi-user.target
\`\`\`
\`systemctl enable --now factory-review\`，再 \`pocketbase migrate up\` 应用迁移。

## 4. Nginx + HTTPS
- 复制 deploy/nginx.conf 到 /etc/nginx/sites-available/ 并软链 enabled
- \`certbot --nginx -d your-domain.com\` 申请证书并自动续签
- \`nginx -t && systemctl reload nginx\`

## 5. 首次初始化
- 访问 https://your-domain.com/_/ 创建超级管理员
- 用 /admin/users 创建各岗位账号

## 6. 备份
- \`crontab -e\` 加：\`0 2 * * * /opt/factory-review/deploy/backup.sh\`
\`\`\`
```

- [ ] **Step 4: Commit**

```bash
git add docs/DEPLOY.md deploy/nginx.conf deploy/backup.sh
git commit -m "docs: add cloud deployment guide, nginx config, backup script"
```

---

## Self-Review 备忘（写计划时已核对）

- **Spec 覆盖**：表1=Phase3 / 产值=Phase4 / 异常=Phase5 / 表2评分+模板+红黄牌=Phase6 / 表3大盘+评审=Phase7 / KPI=Phase8 / 角色权限=Phase2+各 hook / 云部署=Phase9。全部有对应任务。
- **权限一致性**：产值仅 finance（hook+前端）、合作状态需经理审批（hook+UI）、红黄牌经理终审（hook+UI）、评分模板 admin（collection rule+守卫）、采购按工艺过滤（visibleCraft）——四处约束在 collection rule / hook / store / 前端守卫层层对齐。
- **命名一致性**：`gradeFromScore`、`totalFromItems`、`filterByCraft`、`visibleCraft`、`summarizeByCraft`、`onTimeRate`、`useScoresStore.save(factoryId, yearMonth, data)` 等签名在定义与调用处一致。
- **0 陷阱**：金额/产值类 number 字段不设 required，符合现有项目踩坑记录。
- **待执行期验证项**：Task 8.1 的 `onRecordAfter*Success` 钩子命名需在目标 PB 版本实测（Step 2 已含验证与降级说明）。
